export const environment = {
  production: true,
  api: "http://localhost:9090/MoneyMinder/api/",
  // api: location.protocol + "//" + location.host + "/MoneyMinder/api/",
  token: "MM-Safe-Token"
};
